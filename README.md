# browsersync-gulp-example

Example of [Browsersync](https://browsersync.io/) + [Gulp](https://gulpjs.com/).

Command|Description
`yarn build`|Build source code (`src`)
`yarn clean`|Clean the build output (`dist`)
`yarn dev`|Run the local dev server
